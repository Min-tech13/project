local   all             postgres                                peer
local   all             all                                     peer
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

{% for host in groups['postgres_cluster'] %}
host    replication     {{ replication_user }}     {{ hostvars[host]['ansible_default_ipv4']['address'] }}/32    scram-sha-256
host    {{ app_db_name }}    {{ app_db_user }}      {{ hostvars[host]['ansible_default_ipv4']['address'] }}/32    scram-sha-256
{% endfor %}

host    {{ app_db_name }}    {{ app_db_user }}      10.0.0.0/8              scram-sha-256